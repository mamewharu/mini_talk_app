<h3>mini_talk_app</h3>
<%= form_with model: @message do |f| %>
<div id='messages'>
  <% @messages.reverse_each do |message| %>
    <p><%= message.created_at %></p>
    <p><%= message.text %></p>
  <% end %>
</div>

  <%= f.text_field :text %>
  <%= f.submit '送信' %>
<% end %>